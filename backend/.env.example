# Comma-separated allowed CORS origins for the API (the frontend's URL).
LINELAB_CORS_ORIGINS=http://localhost:3000

# Supabase — LineLab project (ref: gsfrghcurkixqmrqghaw).
SUPABASE_URL=https://gsfrghcurkixqmrqghaw.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# Secrets (do not commit real values) — from the Supabase dashboard:
#   Project Settings › API      › service_role key        (server-side writes; bypasses RLS)
#   Project Settings › Database › Connection string (URI)  (includes your DB password)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=
